# Database Configuration
# Internal Railway URL (used by the app running on Railway)
DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway

# Public Railway URL (used for local development/seeding)
DATABASE_PUBLIC_URL=postgresql://postgres:password@your-proxy.proxy.rlwy.net:12345/railway

# Port (Railway sets this automatically)
PORT=8000
